working on research paper assistant RAG, in which recent papers are fetched and stored  in the database in db we store metadata along with the url of that research paper pdf, and we chunk and embed and store embeddings in the vector database that is faiss as of now (laptop RAM is 8GB), then whenever user asks a query searching is done from that database and response is generated in a proper form by LLM.
Also, earlier I was working only on text-based chunking and embeddings, but now I need to work on images as well. 
As most of the images store information related to graphs, plots, charts, and different types of images, etc
How do we deal with that?
what are theapproaches we can use to continue for the task?
if lets say in one paragraph which is at starting of the page, refering to the figure at the end of the image, how d we deal with  that too?
we need to use the light weight models for the task 

